/**
 * Setup global para los tests de frontend.
 *
 * - Registra los matchers de Testing Library (toBeInTheDocument, etc.)
 *   sobre el expect de Vitest mediante @testing-library/jest-dom/vitest.
 */
import '@testing-library/jest-dom/vitest';